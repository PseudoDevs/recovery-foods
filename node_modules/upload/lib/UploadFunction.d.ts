import { UploadOptions, UploadProgressEventListener, UploadResponse } from './Upload';
import FormDataNode from 'form-data';
interface UploadFunctionOptions extends Omit<UploadOptions, 'form' | 'url'> {
    onProgress?: UploadProgressEventListener;
}
export declare function upload(url: string, form: Record<string, string | Blob> | FormData | FormDataNode, options?: UploadFunctionOptions): Promise<UploadResponse>;
export {};
